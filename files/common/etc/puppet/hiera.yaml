---
:backends:
  - 'yaml'

:hierarchy:
  - '%{::hostname}/%{calling_module}'
  - '%{::domain}/%{calling_module}'
  - '%{calling_module}'
  - 'common'

:logger: 'console'

:yaml:
  :datadir: '/etc/puppet/hieradata'
